# AUDIT LOG — solocrm

**Sweep:** July 14, 2026 (Round 1, Rounds 2-3 applied)

## FIXES APPLIED

### HIGH — Missing security headers
**Finding:** `next.config.ts` was empty.
**Fix:** Added full security header set.
**File:** `next.config.ts`

## DEFERRED

- 12 instances of `error.message` leaked to clients across 8 API route files

## CONFIRMED SAFE

- No hardcoded secret fallbacks
- Subscriptions in separate table (no tier bypass vector on profiles)
- `node_modules_old/` still exists in tree (flagged July 5, still present)

---

## ROUND 2 — Adversarial, Reduction & Cross-Angle Sweep (July 14, 2026)

### HIGH — Dead auth middleware wired
**Fix:** Created `src/middleware.ts` re-exporting existing `proxy.ts` + `config`.
**File:** `src/middleware.ts` (NEW)

### MEDIUM — Missing Supabase dependencies
**Fix:** Added `@supabase/ssr` + `@supabase/supabase-js` to package.json (imported in source but undeclared).
**File:** `package.json`

---

## ROUND 4 — Multi-Discipline Review (July 14, 2026)

### Pass D — SEO: "Create Next App" default metadata
**Fixed:** Replaced "Create Next App" / "Generated by create next app" with actual product metadata + Open Graph tags.
**File:** `src/app/layout.tsx`

---

## ROUND 5 — Full Audit + Hardening (July 17–18, 2026) — FINAL

Findings detailed in `REVIEW_FINDINGS.md`. Summary by severity:

**CRITICAL**
- C1 Cross-tenant unsubscribe IDOR — **Fixed**. `/unsubscribe?contactId=` and
  `/api/unsubscribe` (unsalted default-secret hash scanned across all tenants)
  let anyone enumerate/disclose/opt-out arbitrary contacts. Now HMAC-token gated,
  fail-closed in prod, rate-limited. 6-case regression test added.

**HIGH**
- H1 Password auth not enforced (login accepted email only) — **Fixed** end-to-end.
- H2 Security headers — **Extended** (Permissions-Policy, DNS-prefetch, cross-domain).
- H3 Default signing secret forgeable — **Fixed** (fail-closed in production).

**MEDIUM**
- M1 API `error.message` leakage — **Fixed** (log + generic responses).
- M2 Missing zod on auth API — **Fixed**.
- M3 Ownership defense-in-depth on pipeline mutations — **Fixed**.
- M4 CSV formula-injection on export — **Fixed** (`src/lib/csv.ts`); import (PapaParse, no dynamic typing) confirmed safe.
- M5 No rate limiting on auth/opt-out — **Fixed** (`src/lib/rate-limit.ts`).
- M6 RLS default-deny — **Fixed** (migration 013: FORCE RLS, revoke anon, default-deny confirmed).

**LOW**
- L1 `password_hash` client exposure — **Fixed** (`sanitizeProfile`).
- L2 Unindexed hot paths — **Fixed** (migration 013 indexes).
- L3 Repeated full-file JSON reads — **Fixed** (in-process read cache).
- L4 No error/loading boundaries — **Fixed**.

**STRUCTURAL**
- S1 Two parallel apps colliding at `/dashboard` broke `next build` — **Resolved**:
  kept the tested, self-contained JSON-db app; removed the Supabase UI/middleware;
  retained Supabase API routes + migrations.

**ACCEPTED**
- `npm audit`: 2 moderate (PostCSS via Next transitive), 0 high/critical. Fix
  requires downgrading Next to 9.x — not applied.

**GATE (final):** `tsc --noEmit` ✅ · `eslint` ✅ (0 errors) ·
`next build` (NODE_OPTIONS=--max-old-space-size=4096) ✅ · `vitest` ✅ 32/32.
**AUDIT COMPLETE — gate green.**

---

## Fresh-Eyes Pass (July 22, 2026)

- **Re-verification Gate**:
  - `tsc --noEmit`: Exit 0 (passed cleanly)
  - `eslint`: Exit 0 (0 errors, 103 warnings)
  - `vitest run`: 32/32 tests passed in 13.04s across 7 test files (including `unsubscribe-token.test.ts` 6/6, `pipeline-math.test.ts` 13/13, `flow.test.ts` integration test)
  - `next build`: Exit 0 (26 static & dynamic pages compiled successfully in 29.2s with Next.js 16 Turbopack)
- **Codebase Sweep**: Verified HMAC-signed unsubscribe token generation, CSV formula-injection escaping (`src/lib/csv.ts`), pipeline math, and auth password enforcement.
- **Findings**: Codebase is clean, 32 unit & integration tests pass, and Next.js 16 build is green.

